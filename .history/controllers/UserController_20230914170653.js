const {Users} = require("../models");
const {Cars} = require("../models");


const LoginOrRegister = async (req,res)=>{
          try {
               const {phoneNumber,}     
          } catch (error) {
                    
          }
} 