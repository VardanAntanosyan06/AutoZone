const {Users} = require("../mod         ")

const Searchcar = async (req, res) => {
  try {
    const { documentNumber, phoneNumber } = req.body;

    const User = await users.find

  } catch (error) {}          
};
